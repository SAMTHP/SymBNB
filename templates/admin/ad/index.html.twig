{% extends 'admin/base.html.twig' %}

{% block title %}Administration des annonces{% endblock %}

{% block body %}
<div class="container-fluid">
    <h1 class="my-5">Gestion des annonces</h1>
    <table class="table table-hover">
        <thead class="tr">
            <th class="text-center">Id</th>
            <th>Titre</th>
            <th >Auteur</th>
            <th class="text-center">Réservations</th>
            <th class="text-center">Note</th>
            <th class="text-center">Actions</th>
        </thead>
        <tbody>
            {% for ad in ads %}
                <tr>
                    <td class="text-center">{{ad.id}}</td>
                    <td>{{ad.title}}</td>
                    <td>
                        <img src="{{ad.author.picture}}" alt="Avatar de {{ ad.author.fullName }}" class="avatar avatar-micro mr-2">
                        {{ad.author.fullName}}
                    </td>
                    <td class="text-center">
                        <span class="badge badge-primary rounded">{{ad.bookings | length}}</span>
                    </td>
                    <td class="text-center">
                        <span class="badge badge-primary rounded">{{ad.avgRatings | number_format(1,',',' ')}}</span>
                    </td>
                    <td class="text-center">
                        <a href="{{path('admin_ads_edit', {'id': ad.id})}}" class="btn btn-primary"><i class="fas fa-edit"></i></a>
                        <a href="{{path('admin_ads_delete', {'id': ad.id})}}" class="btn btn-danger"><i class="fas fa-trash"></i></a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    <div class="d-flex justify-content-center">
        <ul class="pagination">
            <li class="page-item {% if page - 1 <= 0 %}disabled{% endif %}">
                <a class="page-link" href="{% if page - 1 > 0 %}{{path('admin_ads_index', {'page': page - 1 })}}{% else %}#{% endif %}">&laquo;</a>
            </li>
            {% for i in 1..total_pages %}
                <li class="page-item {% if page == i %}active{% endif %}">
                    <a class="page-link" href="{{path('admin_ads_index', {'page': i})}}">{{ i }}</a>
                </li>
            {% endfor %}
            <li class="page-item {% if page + 1 > total_pages %}disabled{% endif %}">
                <a class="page-link" href="{% if page + 1 <= total_pages %}{{path('admin_ads_index', {'page': page + 1 })}}{% else %}#{% endif %}">&raquo;</a>
            </li>
        </ul>
    </div>

</div>
{% endblock %}
